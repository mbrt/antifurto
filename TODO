TODO LIST
---------

- In Picture.hpp use an std::vector<unsigned char> instead of cv::Mat;
  provide conversion operators to InputArray and OutputArray
- Refactor of config
  - move Configuration class out of config namespace
  - move parsing facilities to a ConfigParser class
  - add parsing of command line
- Add templating mechanism to support config file generation for yowsup and
  dropbox uploader
- Kill subprocess
- Packaging system
